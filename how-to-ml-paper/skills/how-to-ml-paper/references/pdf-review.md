# Rendered PDF review

Compile the paper with its declared build system. Inspect every rendered page at normal reading size. Extract PDF text and search for unresolved citations or references such as `??`.

## Venue-enforced checks

Apply only requirements retrieved from current official venue material. Check paper size, page limit, fonts, anonymity, required sections, template version, file size, and any required format checker.

## Visual and cross-reference QA

Check equations and tables for margin overflow. Read figure labels at normal size. Verify axes, units, legends, captions, and text references. Inspect stranded headings, accidental sparse pages, inconsistent notation, citation rendering, and float placement.

Every caption should state what the reader should notice. Figure 1 should communicate the primary mechanism or input/output story to a domain expert when read with its caption. Limit its concept count to what that purpose needs.

Report exact PDF page numbers. Distinguish venue violations from submission-readiness heuristics.
